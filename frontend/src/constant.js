export const server = import.meta.env.VITE_BACKEND_URL;

export const AUTH_URL = `${server}/api/v1/auth`;

export const USER_URL = `${server}/api/v1/user`;

export const ADMIN = `${server}/api/v1/admin`;

export const COURSE = `${server}/api/v1/course`;
